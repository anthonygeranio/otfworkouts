{
  "name": "class-preview-notifier",
  "private": true,
  "scripts": {
    "dev": "wrangler dev --test-scheduled",
    "deploy": "wrangler deploy",
    "db:init:local": "wrangler d1 execute class-preview --local --file=schema.sql",
    "db:init": "wrangler d1 execute class-preview --remote --file=schema.sql",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260924.1",
    "typescript": "^7.0.2",
    "wrangler": "^4.138.0"
  }
}
